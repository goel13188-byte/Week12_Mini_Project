# 🚀 AI Image Classifier (Week 12 Mini-Project)

A simple yet powerful end-to-end Image Classification app built with **PyTorch** and **Gradio**.

## 📌 Project Overview
- **Goal:** Classify images into 10 categories (CIFAR-10).
- **Model:** Convolutional Neural Network (CNN).
- **Interface:** Web-based UI via Gradio.

## 📂 Project Structure
- `src/`: Core logic (model architecture, preprocessing, and prediction).
- `models/`: Saved model weights (`.pth` file).
- `app/`: Gradio UI script.
- `requirements.txt`: List of necessary libraries.

## 🛠️ How to Run
1. **Create Virtual Environment:**
   ```bash
   python -m venv venv
   .\venv\Scripts\activate